machine:

  environment:
    IMG_NAME: TestCircleCi
    MAX_STAGE: 2

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get -y install git quilt kpartx realpath qemu-user-static debootstrap zerofree pxz zip 

  post:
    - sudo -E ./build.sh
